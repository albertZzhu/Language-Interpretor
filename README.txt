Boru Zhu
Scanner.py: The python file that read in every string in the file stream and convert them into tokens.
Main.py: The python file that drives the whole program and handle the token output.
Core.py: The python file that contains all the avaliable tokens in Enum form.

No any special fetures inside the whole project, this program performs as describes in the project description.
